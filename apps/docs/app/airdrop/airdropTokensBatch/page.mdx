import { Callout } from 'nextra/components'
import PropsTable from '../../PropsTable.jsx'

# `airdropTokensBatch`

This function allows you to implement airdrop functionality in batches for large sized airdrops(Greater than 30-40 recipients)
## Usage

``` jsx filename="page.tsx"
import { useConnection, useWallet } from "@solana/wallet-adapter-react";
import { airdropTokensBatch } from "@okito/sdk";

const {connection} = useConnection();
const wallet = useWallet();
const mintAddress = ""; //mint address for the token 
const recipients = [{
        address: "", //the address to which the token is being airdropped
        amount: 10.5 //the amount of the token to be airdropped
    }]

const airdropResult = await airdropTokensBatch(connection,wallet,mintAddress,recipients)

```

<Callout type="info">
  Airdrop will proceed in batches, hence expect long transaction time.
</Callout>


## Props

<PropsTable
  rows={[
    {
      name: "connection",
      type: "Connection",
      description: "The connection object to the Solana blockchain.",
      default: "—",
    },
    {
      name: "wallet",
      type: "SignerWallet",
      description: "The sender's wallet object, conforming to the SignerWallet interface.",
      default: "—",
    },
    {
      name: "mint",
      type: "string",
      description: "The mint address of the token to be airdropped.",
      default: "—",
    },
    {
      name: "recipients",
      type: "AirdropRecipient[]",
      description: "Array of recipient objects, each with an address and amount.",
      default: "[]",
    },
    {
      name: "config",
      type: "AirdropTokenBatch",
      default: "—",
    },
  ]}
/>


### `AirdropTokenBatch`

<PropsTable
  rows={[
    {
      name: "batchSize",
      type: "number",
      description: "Batch size for airdrop. Number of recipients per batch.",
      default: "25",
    },
    {
      name: "delayBetweenBatches",
      type: "number",
      description: "Delay in milliseconds between batches.",
      default: "2000",
    },
    {
      name: "enableLogging",
      type: "boolean",
      description: "Whether to enable logging for progress and errors.",
      default: "true",
    },
    {
      name: "airdropConfig",
      type: "AirdropConfig",
      description: "Additional airdrop configuration (see AirdropConfig for details).",
      default: "—",
    },
  ]}
/>




## Response

The response object contains: **`AirdropResult`**

<PropsTable
  rows = {[
    {
        name:"OperationResult",
        type:"OperationResult",
        description:"The result of a transaction in Okito.",
        default:"—"
    },
    {
      name:"recipientsProcessed",
      type:"number | null",
      description:"The number of recipients processed",
      default:"—"
    },
    {
      name:"accountsCreated",
      type:"number | null",
      description:"The number of accounts created",
    },
    {
      name:"totalAmountSent",
      type:"string | null",
      description:"The total amount of the token sent",
      default:"—"
    },

  ]}
  />

