import PropsTable from "../PropsTable.jsx"
import {Callout} from 'nextra/components'

# BaseOperationConfig



This is the most important configuration of Okito. This describes the configuration of any transaction that will be done using Okito.


<Callout type="info">
Most of the configs present in Okito is an extension to `BaseOperationConfig`.
</Callout>



## Types


<PropsTable
    rows={[{
        name:"maxRetries",
        type:"number",
        description:"The maximum number of times to retry the transaction.",
        default:"3"
    },
    {
        name:"timeoutMs",
        type:"number",
        description:"The timeout in milliseconds for the transaction.",
        default:"30000"
    },
    {
        name:"confirmationStrategy",
        type:"'processed' | 'confirmed' | 'finalized'",
        description:"The confirmation strategy for the transaction.",
        default:"processed"
    },
    {
        name:"priorityFee",
        type:"number",
        description:"The priority fee for the transaction.",
        default:"0"
    },
    {
        name:"enableLogging",
        type:"boolean",
        description:"Whether to enable logging for the transaction.",
        default:"false"
    },
    {
        name:"enableSimulation",
        type:"boolean",
        description:"Whether to enable simulation for the transaction.",
        default:"false"
    },
    {
        name:"validateBalance",
        type:"boolean",
        description:"Whether to validate the balance of the wallet before the transaction.",
        default:"false"
    }
    ]}
/>