import { Callout } from 'nextra/components'
import PropsTable from '../../initialising/PropsTable.jsx'

## `getTransactionHistory`

This function allows you to fetch the transaction history of a wallet address

## Snippet

``` jsx filename="page.tsx"
import { useConnection } from "@solana/wallet-adapter-react";
import { getTransactionHistory } from "@okito/sdk";

const {connection} = useConnection();

const wallet = useWallet();

const options = {
        limit: 5, //The number of transactions to fetch
        before: "", //The signature to fetch from
        until: "", //The signature to fetch to
        commitment: "processed" //The commitment level to use
}

const transactionHistory = await getTransactionHistory(connection,address,options)
```

<Callout type="warning">
The default values of the params( **limit = 20, commitment = "confirmed"** ) will be used if not provided.
</Callout>


## Props

<PropsTable
  rows={[
    {
      name: "Connection",
      type: "Connection",
      description: "The connection object to the blockchain.",
      default: "—",
    },
    {
      name: "address",
      type: " PublicKey | string ",
      description: "The address of the account for which transactions are being fetched.",
      default: `—`,
    },
    {
      name: "options",
      type: "TransactionHistoryOptions",
      description: "The options for fetching the transaction history.",
      default: `—`,
    },
  ]}
/>



## Response

<PropsTable
  rows = {[
    {
      name:"transactions",
      type:"  ParsedTransactionWithMeta[] | null",
      description:"The transactions fetched",
      default:"—"
    },
    {
      name:"success",
      type:"boolean",
      description:"Whether the transactions were fetched successfully",
    },
    {
      name:"error",
      type:"string | undefined",
      description:"The error message if the transactions were not fetched successfully",
      default:"—"
    },{
     name:"signatures",
     type:"ConfirmedSignatureInfo[] | undefined",
     description:"The raw signatures fetched",
     default:"—"
    },{
        name:"hasMore",
        type:"boolean | undefined",
        description:"Whether there are more transactions to fetch",
        default:"—"
    }
  ]}
/>





## Types 

### `ParsedTransactionWithMeta`

<PropsTable
 rows={[
    {
        name:"slot",
        type:"number",
        description:"The slot during which the transaction was processed",
        default:"—"
    },
    {
        name:"transaction",
        type:"ParsedTransaction",
        description:"The transaction details",
        default:"—"
    },
    {
        name:"meta",
        type:"ParsedTransactionMeta | null",
        description:"The metadata produced from the transaction",
        default:"—"
    },
    {
        name:"blockTime",
        type:"number | null",
        description:"The unix timestamp of when the transaction was processed",
        default:"—"
    },
    {
        name:"version",
        type:"TransactionVersion | undefined",
    }
 ]}
/>





### `ConfirmedSignatureInfo`


<PropsTable
 rows={[
    {
        name:"signature",
        type:"string",
        description:"The signature of the transaction",
        default:"—"
    },
    {
        name:"slot",
        type:"number",
        description:"The slot during which the transaction was processed",
        default:"—"
    },
    {
        name:"err",
        type:"TransactionError | null",
        description:"The error produced from the transaction",
        default:"—"
    },
    {
        name:"memo",
        type:"'string' | null",
        description:"The memo associated with the transaction(if any)",
        default:"—"
    },
    {
        name:"blockTime",
        type:"number | null",
        description:"The unix timestamp of when the transaction was processed",
        default:"—"
    },
    {
        name:"confirmationStatus",
        type:"'processed' | 'confirmed' | 'finalized' | undefined",
        description:"The confirmation status of the transaction",
        default:"—"
    }
 ]}
/>
