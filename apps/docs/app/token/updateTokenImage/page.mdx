import { Callout } from 'nextra/components'
import PropsTable from '../../PropsTable.jsx'

# `updateTokenImage`

Updates the metadata URI (image URL) for an existing token.

## Usage

``` jsx filename="page.tsx"
import { useConnection, useWallet } from "@solana/wallet-adapter-react";
import { updateTokenImage } from "@okito/sdk";

const {connection} = useConnection();
const wallet = useWallet();
const mintAddress = ""; // Token mint address
const newImageUrl = ""; // New image URL

const result = await updateTokenImage(wallet, connection, mintAddress, newImageUrl, {
  enableLogging: true,
  maxRetries: 3,
  confirmationStrategy: 'confirmed'
});
```

## Parameters

<PropsTable
  rows={[
    { name: "connection", type: "Connection", description: "Solana connection instance.", default: "—" },
    { name: "wallet", type: "SignerWallet", description: "Wallet with authority to update metadata.", default: "—" },
    { name: "mint", type: "string", description: "Token mint address.", default: "—" },
    { name: "newImageUrl", type: "string", description: "New image URL to set as the token URI.", default: "—" },
    { name: "config", type: <a href="/OperationConfig">OperationConfig</a>, description: "General options such as logging, retries, timeouts, and confirmation level.", default: "Defaults" },
  ]}
/>

## Response

Returns <a href="/OperationResult">OperationResult</a>.

<PropsTable
  rows={[
    { name: "success", type: "boolean", description: "Whether the update succeeded." },
    { name: "mintAddress", type: "string | null", description: "Mint address for reference." },
    { name: "transactionId", type: "string | null", description: "Signature of the update transaction." },
    { name: "error", type: "string | null", description: "Error message if any." },
    { name: "estimatedFee", type: "number | null", description: "Estimated fee (if available)." },
    { name: "actualFee", type: "number | null", description: "Actual network fee (if available)." },
    { name: "confirmationTime", type: "number | null", description: "Time to confirm in ms." },
  ]}
/>

